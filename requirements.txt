# Requisitos del proyecto (agente + simulador + modelos)
# Nota: `torch` pesa mucho. Para build solo-CPU sustituye la línea por:
# torch --index-url https://download.pytorch.org/whl/cpu

numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
paho-mqtt>=2.0
xgboost>=2.0
torch>=2.2
